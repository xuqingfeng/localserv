```
Usage of localserv:
  -ca string
    	CA file (ca.pem)
  -dir string
    	directory (default "./")
  -host string
    	host (127.0.0.1 / 0.0.0.0) (default "127.0.0.1")
  -key string
    	key file (key.pem)
  -port int
    	port (default 8000)
```